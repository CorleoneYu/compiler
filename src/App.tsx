import React from 'react';
import Router from './component/router';

// worker 线程
// import './worker/index';

const App: React.FC = () => {
  return (
    <div className="App">
      <Router />
    </div>
  );
};

export default App;
